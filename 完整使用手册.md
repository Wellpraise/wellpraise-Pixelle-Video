# Pixelle-Video 完整使用手册

> 本文档包含项目说明、注意事项、使用方法、故障排查等全部内容。
> 更新日期：2026-08-09

---

## 一、项目简介

Pixelle-Video 是一款 AI 驱动的自动化视频生成平台，支持从文字生成脚本、生成图片或视频，并自动合成最终视频。

### 核心功能
- 文字生成视频脚本
- 调用 AI 生成配图/视频素材
- 自动剪辑合成最终视频
- 支持多种视频模板

### 技术架构
- **后端服务**: FastAPI (Python)
- **前端界面**: Streamlit
- **虚拟环境**: Python 3.13.12 + uv

---

## 二、已配置信息

### 2.1 LLM 服务（已配置，可直接使用）

| 项目 | 值 |
|------|-----|
| 服务 | Agnes AI |
| Base URL | https://apihub.agnes-ai.com/v1 |
| 模型 | agnes-2.5-flash |
| API Key | `wk-3UTcT7xM290dZ8vo4p8Hu48XYjGmwU7LotOieGbrzMuMn90I` |

**可用模型列表：**
- `agnes-2.0-flash` — 快速对话模型
- `agnes-2.5-flash` — 推荐模型，性能与速度平衡
- `agnes-2.5-pro` — 专业版，更强推理能力
- `agnes-2.5-pro-alpha` — 专业版 alpha
- `agnes-image-2.0-flash` — 图像生成
- `agnes-image-2.1-flash` — 图像生成（新版）
- `agnes-video-v2.0` — 视频生成

### 2.2 服务地址

| 服务 | 地址 | 用途 |
|------|------|------|
| Web UI | http://localhost:8501 | 主要操作界面 |
| API | http://localhost:8000 | 后端服务 |
| API 文档 | http://localhost:8000/docs | API 接口说明 |

### 2.3 关键路径

| 项目 | 路径 |
|------|------|
| 项目目录 | `/Users/inbest/Desktop/AI/workbuddy/pixelle-video/` |
| 配置文件 | `config.yaml` |
| 输出目录 | `output/` |
| 虚拟环境 | `.venv/` |
| 启动脚本 | `start.sh` |
| 配置打包 | `/Users/inbest/Desktop/pixelle-video-config.tar.gz` |

---

## 三、启动方法

### 方法一：使用启动脚本（推荐）

```bash
cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video
./start.sh
```

### 方法二：手动启动

打开两个终端：

**终端 1 — 启动 API 服务：**
```bash
cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video
.venv/bin/python api/app.py --host 0.0.0.0 --port 8000
```

**终端 2 — 启动 Web UI：**
```bash
cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video
.venv/bin/python -m streamlit run web/app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true
```

### 方法三：后台运行

```bash
cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video

# 后台启动 API
.venv/bin/python api/app.py --host 0.0.0.0 --port 8000 > /tmp/pixelle-api.log 2>&1 &

# 后台启动 Web UI
.venv/bin/python -m streamlit run web/app.py --server.port 8501 --server.address 0.0.0.0 --server.headless true > /tmp/pixelle-web.log 2>&1 &
```

---

## 四、使用流程

### 4.1 基本使用步骤

1. 启动服务（参考第三节）
2. 浏览器访问 http://localhost:8501
3. 输入视频主题或脚本内容
4. 选择视频模板和风格
5. 点击生成，等待完成
6. 视频输出到 `output/` 目录

### 4.2 配置修改

如需修改配置，编辑配置文件：
```
/Users/inbest/Desktop/AI/workbuddy/pixelle-video/config.yaml
```

常用修改项：
- `llm.api_key` — 更换 API Key
- `llm.model` — 切换模型
- `llm.base_url` — 更换 API 服务地址
- `comfyui.runninghub_api_key` — 配置 RunningHub（如需图像/视频生成）

---

## 五、注意事项

### ⚠️ 重要注意事项

1. **API Key 安全**
   - 请勿将 `config.yaml` 提交到 Git 仓库
   - 建议将 `config.yaml` 添加到 `.gitignore`
   - API Key 请妥善保管，避免泄露

2. **端口占用**
   - API 服务默认使用 8000 端口
   - Web UI 默认使用 8501 端口
   - 如端口被占用，修改 `config.yaml` 或启动参数

3. **虚拟环境依赖**
   - 项目使用 `.venv/` 虚拟环境
   - 确保在正确目录下启动服务
   - 如需重新安装依赖：
     ```bash
     cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video
     .venv/bin/pip install -r requirements.txt
     ```

4. **配置打包文件**
   - 配置文件已打包在 `/Users/inbest/Desktop/pixelle-video-config.tar.gz`
   - 如需在其他机器使用，解压后替换 `config.yaml` 即可
   - 注意：打包文件中不包含虚拟环境，需要重新安装依赖

### ⚠️ 可选功能限制

1. **图像/视频生成需要额外配置**
   - 当前仅配置了 LLM 服务
   - 图像生成：需配置 RunningHub 或本地 ComfyUI
   - 视频生成：需配置 RunningHub 或本地 ComfyUI

2. **RunningHub 配置（推荐）**
   - 访问 https://runninghub.cn/ 注册账号
   - 获取 API Key
   - 在 Web UI → ⚙️ 系统配置 中填入
   - 或修改 `config.yaml` 的 `comfyui.runninghub_api_key`

3. **本地 ComfyUI（可选）**
   - 需要 NVIDIA 显卡（6GB+ 显存）
   - 需单独部署 ComfyUI 服务
   - 配置 `comfyui.comfyui_url` 指向本地 ComfyUI

---

## 六、故障排查

### 6.1 服务无法启动

**问题：端口被占用**
```bash
# 查看端口占用
lsof -i :8000
lsof -i :8501

# 杀死占用进程
kill -9 <PID>
```

**问题：依赖未安装**
```bash
cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video
.venv/bin/pip install -r requirements.txt
```

### 6.2 API Key 认证失败（401）

**检查项：**
1. API Key 是否正确复制（无多余空格）
2. Base URL 是否正确
3. 确认 API Key 未过期

**测试 API Key：**
```bash
curl -X POST https://apihub.agnes-ai.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{"model": "agnes-2.5-flash", "messages": [{"role": "user", "content": "hello"}]}'
```

### 6.3 图像/视频生成失败

**可能原因：**
1. RunningHub API Key 未配置
2. RunningHub 账户余额不足
3. 本地 ComfyUI 未启动或地址错误

**解决方法：**
- 检查 `config.yaml` 中的 `comfyui.runninghub_api_key`
- 确认 RunningHub 账户状态
- 如使用本地 ComfyUI，确保服务已启动

### 6.4 Web UI 打不开

**检查步骤：**
```bash
# 确认 Web UI 服务是否运行
curl http://localhost:8501/

# 查看进程
ps aux | grep streamlit
```

---

## 七、维护与备份

### 7.1 配置文件备份

配置文件已打包保存：
```
/Users/inbest/Desktop/pixelle-video-config.tar.gz
```

包含文件：
- `config.yaml` — 主配置文件
- `README_CONFIG.md` — 配置说明
- `快速启动指南.md` — 快速启动命令
- `Pixelle-Video配置总结.md` — 配置总结
- `start.sh` — 启动脚本

### 7.2 定期维护建议

1. 定期检查 API Key 有效期
2. 定期清理 `output/` 目录下的旧视频文件
3. 备份配置文件到云端或移动设备

---

## 八、常见问题 FAQ

**Q: 如何更换 API Key？**
A: 编辑 `config.yaml`，修改 `llm.api_key` 和 `api_providers.openai.api_key` 字段。

**Q: 如何更换模型？**
A: 修改 `config.yaml` 的 `llm.model` 字段，改为可用模型名称（如 `agnes-2.5-pro`）。

**Q: 如何启用图像生成功能？**
A: 配置 RunningHub API Key，或在本地部署 ComfyUI。

**Q: 视频输出到哪里？**
A: 输出到 `output/` 目录。

**Q: 如何完全停止服务？**
A:
```bash
# 查找并杀死进程
pkill -f "api/app.py"
pkill -f "streamlit run web/app.py"
```

---

## 九、快速参考

### 常用命令

```bash
# 启动服务
cd /Users/inbest/Desktop/AI/workbuddy/pixelle-video
./start.sh

# 检查服务状态
curl http://localhost:8501/
curl http://localhost:8000/health

# 停止服务
pkill -f "api/app.py"
pkill -f "streamlit run web/app.py"

# 查看日志
tail -f /tmp/pixelle-api.log
tail -f /tmp/pixelle-web.log
```

### 配置修改

```bash
# 编辑配置
vim /Users/inbest/Desktop/AI/workbuddy/pixelle-video/config.yaml

# 备份配置
cp config.yaml config.yaml.bak
```

---

**文档版本**: 1.0  
**最后更新**: 2026-08-09  
**维护者**: WorkBuddy
